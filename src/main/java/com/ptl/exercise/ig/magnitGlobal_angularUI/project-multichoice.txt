Q.  a guard should run before navigating to a child route.
A:  canActivateChild

Q.  What is an image sprinte?
A:  Combining multiple images in  a single image file.

Q.  HTML5: <required> <Submit> button
A:  The "Required" attribute will check if the text field is empty on the client side when the form is submitted
A:  The submit button can be clicked even if the text field is empty.

Q:  app.component.ts name = "HELLO" condition = true
A:  HELLO
    Name: hello

Q:  value for margin css style
A:  margin: top right bottom left;  (choose according to this order)

Q:  select any invalid declarations for a pseduo-element
A:  span::last-line{...}
A:  .header::after-first-line{...}

Q:  select the valid css3 transofrmation statemetns
A:  matrix()
A:  rotate()

Q:  div {resize: horizental}
A:  It makes the div resizable by the user.

Q:  define a token such that it is same as string token but generates unique value every time
A:  Injector.create

Q:  select all the new items introduced in HTML5
A:  Drag and Drop
A:  2D drawing on a web page
A:  Time media playback
A:  New semantics like <section>, <article>, <footer>, etc.

Q:  valid statements
A:  a[href^="#"] { background-color: gold; }
A:  a[href] { background-color: gold; } 

Q:  Add shadow to text
A:  h1 { text-shadow: 5px 5px; }

Q:  app-card selector
A:  <app-card>
    <div class="header"><h1>Angular</h1></div>
    </app-card>

Q:  Which code produces the following? Width and height are 100px. (Coding is Fun)
A:  <fieldSet style="height:100px;width:100px"><legend>Coding is Fun</Legend></fieldSet>

Q:  select all the new items introduced in HTML5;
A:  Drag and Drop
A:  2D drawing on a web page
A:  Time media playback
A:  New Semantics like <section>,<article>,<footer> etc

