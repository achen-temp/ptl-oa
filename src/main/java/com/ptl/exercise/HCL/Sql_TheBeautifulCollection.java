package com.ptl.exercise.HCL;

public class Sql_TheBeautifulCollection {

    /**
     * SELECT
     *      CASE
     *          WHEN (RED = GREEN AND GREEN = BLUE) THEN 'GOOD'
     *          WHEN (RED = GREEN OR RED = BLUE OR GREEN = BLUE) THEN 'BAD'
     *          ELSE 'WORSE'
     *      END AS OUTPUT
     * FROM COLLECTION;
     */
}
